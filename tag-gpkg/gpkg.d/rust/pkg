# vim: ft=sh :

_gpkg_version() {
	echo "latest"
}

_gpkg_install() {
	if command -v rustup >/dev/null; then
		rustup update
	else
		_gpkg_fetch https://sh.rustup.rs | sh -s -- --no-modify-path
	fi
}

_gpkg_uninstall() {
	rustup self uninstall
}
