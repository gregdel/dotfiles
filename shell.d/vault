# vim: ft=sh :

vault-open() {
	[ "$VAULTS_DIR" ] || {
		_log_err "Missing vaults dir, please setup the VAULTS_DIR variable"
		return 1
	}

	vault_name=$1
	[ "$vault_name" ] || {
		_log_err "Missing vault name"
		return 1
	}

	gocryptfs -fg "$VAULTS_DIR/.$vault_name" "$VAULTS_DIR/$vault_name"
}
