# vim: ft=sh :

fpass() {
	path_dir=$HOME/.password-store
	password=$(find "$path_dir" -type f -name '*.gpg' \
		| sed "s/.gpg//" \
		| sed "s;$path_dir/;;" \
		| fzf --reverse --tac)
	[ "$password" ] || return 0
	pass show "$@" "$password"
}
