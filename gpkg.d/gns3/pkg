# vim: ft=sh :

bin_name=gns3-gui
bin_version=2.2.34

_gpkg_install() {
	_gpkg_log_info "Installing gns3-gui with pipx"
	pipx install "$bin_name==$bin_version"
	pipx inject "$bin_name" PyQt5
}

_gpkg_uninstall() {
	pipx uninstall "$bin_name"
}
