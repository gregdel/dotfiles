# vim: ft=sh :

git_url="https://github.com/neovim/neovim.git"
git_version="v0.3.0"

_gpkg_install() {
	_gpkg_git_clone "$git_url" "$git_version" "$BUILD_DIR_PATH"
	_gpkg_log "not implemented"

	# Debian stuff, let's work on this later
	# # Install dependencies
	# sudo apt update
	# sudo apt install -y libtool libtool-bin autoconf automake cmake g++ pkg-config unzip

	# # Make
	# rm -rf "$BUILD_DIR/build"
	# make clean
	# make CMAKE_BUILD_TYPE=Release
	# # Make install
	# sudo make install
}

_gpkg_uninstall() {
	_gpkg_log "not implemented"
}
