#!/bin/sh
set -e

file="$(date +'/tmp/%d_%m_%y#%H:%M:%S.png')"

grim -g "$(slurp)" "$file"

wl-copy --primary "$file"

notify-send \
	--icon=folder-black-pictures \
	--urgency=low \
	--expire-time=1000 \
	"Screenshot" \
	"Saved to $file"

